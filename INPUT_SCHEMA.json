{
  "title": "SEO audit tool input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Web URL",
      "type": "string",
      "description": "URL of webpage, where we run SEO audit",
      "prefill": "https://apify.com"
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "number",
      "description": "Maximum concurrent open pages",
      "default": "10",
      "prefill": "10"
    }
  },
  "required": ["startUrl", "maxConcurrency"]
}
